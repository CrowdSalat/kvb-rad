kind: pipeline
type: docker
name: build
steps:
  - name: build jar
    image: openjdk:14-jdk-slim
    commands:
      - ./mvnw package -Dmaven.test.skip=true
      - docker build -t crowdsalat/kvb-rad
